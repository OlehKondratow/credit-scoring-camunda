# Katalog `data/`

- **`train.csv`** — opcjonalnie: pobierz z [credit-scoring](https://github.com/0leh-kondratov/credit-scoring); plik nie jest commitowany (`.gitignore`).
- **`bik-synthetic-snapshot.example.json`** — fikcyjny przykład payloadu zewnętrznego źródła informacji kredytowej (mapowanie na `score_bki` / `bki_request_cnt`); wyłącznie na potrzeby dokumentacji i mocków, **nie** produkcyjna odpowiedź żadnego API.

Szczegóły kontraktu pól i zastrzeżenia: **`docs/bik-kontekst.md`**.
